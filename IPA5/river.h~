#ifndef RIVER_H
#define RIVER_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

char *readFile(char *fptr, int *N);
int checkUp(int curr, int *arr, int N);
int checkDown(int curr, int *arr, int N);
int crossRiver(int current, int *array, int N);
int findBestMove(int current, int *array, int N, int lastMove);

#endif
